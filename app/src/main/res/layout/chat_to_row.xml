<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp">

    <TextView
        android:id="@+id/tvToRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="8dp"
        android:layout_marginStart="8dp"
        android:text="This is my message that will wrap into multiple lines and keep on going"
        android:maxWidth="240dp"
        android:textColor="@color/black"
        android:background="@drawable/shape_corner_white"
        android:padding="16dp"
        app:layout_constraintStart_toEndOf="@+id/ivPhotoToRow"
        app:layout_constraintTop_toTopOf="@+id/ivPhotoToRow" />

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/ivPhotoToRow"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginLeft="8dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        app:civ_border_color="@android:color/white"
        android:src="@drawable/user_pic"
        app:civ_border_width="2dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@android:drawable/btn_star_big_on" />


</androidx.constraintlayout.widget.ConstraintLayout>